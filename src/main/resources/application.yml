spring:
  application:
    name: LojadeProdutosDiversos 
  datasource:
    url: jdbc:postgresql://localhost
    username: 
    password: 
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
